FROM ubuntu:26.04
RUN export DEBIAN_FRONTEND=noninteractive \
    && apt-get update \
    && apt-get install -y \
    clang-format-21 \
    git \
    && rm -rf /var/lib/apt/lists/* /tmp/* /var/tmp/*

ADD . /code

